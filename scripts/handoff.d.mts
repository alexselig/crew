// Types for the handoff brief generator, which stays plain ESM JavaScript so it
// can be run directly with node (and from a launchd job) without a build step.
export declare function rations(count: number, total: number): number[]
